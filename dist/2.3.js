"use strict";
{
    // const rollNumbers: number[] = [2,3,5]
    const rollNumbers = [2, 3, 5];
    // const mentors: string[] = ["rahat", "mortuza", "mahdi"]
    const mentors = ["rahat", "mortuza", "mahdi"];
    // const boolArray: boolean[] = [true, false, true]
    const boolArray = [true, false, true];
    // array of object procedure by generic
    const user = [
        {
            name: "rahat",
            age: 100
        },
        {
            name: "mahdi",
            age: 150
        }
    ];
    const add = (x, y) => x + y;
    add(30, 20);
    const munush = ["Mr. X", "Mr. y"];
    const userWithId = [1234, { name: "rahat", email: "rahat@gmail.com" }];
}
